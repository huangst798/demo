name: Docker

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
env:
    IMAGE_NAME: myhello
  workflow_dispatch:
jobs:
  build:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v2
      - name: install
        run: |
        bash docker.sh